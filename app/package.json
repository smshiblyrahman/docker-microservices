{
  "name": "secure-vault-app",
  "version": "1.0.0",
  "description": "Hardened Node.js microservice reading Azure Key Vault secrets via CSI driver",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
